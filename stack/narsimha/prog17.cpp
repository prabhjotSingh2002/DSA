//implementing Multiple m stack in one array

#include<iostream>
using namespace;

int arr[200]={0};

void push(int stackid,int data)
{
    if(top[i]==base[i+1])
    {
        stack is full
    }
    top[i]=top[i]+1;
    arr[top[i]]=data;
}

void pop(int stackid)
{
    if(top[i]==base[i])
    {
        print i stack is empty
    }
    return arr[top[i]--];
}

int main()
{
    
}